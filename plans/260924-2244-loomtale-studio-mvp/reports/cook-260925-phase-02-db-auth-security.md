# Phase 02 cook report: DB, auth, RBAC, audit, storage, secrets, backups

Branch: `feat/db-auth-security-foundation`. Final commit after review fixes: `36c3354c7660912ccb824265c0f2f12bc7162090` (original implementation: `42befbab794b41816057cd0f887b48adeee82126`; docs follow-up: `1580aa3`; review fixes: `36c3354`). Not pushed, not merged (controller merges).

## What was built

- **DB roles + tenancy schema.** `deploy/postgres/init-roles.sh` (docker-entrypoint-initdb.d) creates `loomtale_owner`/`loomtale_app` from Docker secrets on first boot; `db/migrations/20260925010000_db_roles.sql` grants the split (owner full DDL, app CRUD-by-default-privilege only, no DDL) and fails fast if the roles are missing. `20260925010100_core_tenancy.sql` adds `tenants`, `users`, `memberships`, `sessions`, `rate_limit_buckets`, `tenant_quotas`. `20260925010200_assets.sql`, `..._secrets.sql`, `..._ops.sql` (backup_runs), `..._audit.sql` (trigger-enforced immutable `audit_log`, refusing `goose down`) follow. UUIDv7 generated in Go (`api/internal/db/idconv`). `scripts/lint-tenant-queries.sh` fails CI if a query against a tenant table lacks `tenant_id = @tenant_id` (one documented exception via `lint-tenant-queries:allow`).
- **`tools/tenantctx`** — a separate Go module (`tools/go.mod`) with a `go/analysis` analyzer flagging `TenantID:`/`TenantId:` struct fields populated directly from a request's `Params`/`Body`; wired into `make lint`, which builds it and runs it against `api/...`. Deliberately syntactic (own-expression scope, not full data flow), documented as a scoped tradeoff; testdata covers a violation, a `tenantctx:allow`-annotated exception, and the safe (`tenant.FromCtx`-sourced) form.
- **Envelope encryption** (`api/internal/crypto/envelope`): AES-256-GCM, random per-record DEK wrapped by a KEK loaded from `/run/secrets/master_key` (32B base64, fails fast if absent/malformed), AAD = `tenant_id|kind|record_id`, `key_id` fingerprint for rotation. Property-style tests cover roundtrip, tamper detection (ciphertext, wrapped DEK, AAD mismatch), key-id mismatch, and per-call randomization. Not yet wired to an HTTP route (no secrets are created via the API in this phase — reused by phase 4/10 as planned).
- **Auth** (`api/internal/auth`, `authapi`): argon2id (m=64MiB,t=3,p=2) with PHC-string storage and rehash-on-login-if-outdated; session store (32B random token, only SHA-256 persisted); `__Host-lt_sess` cookie (HttpOnly, Secure, SameSite=Lax, 12h idle / 7d absolute); login/logout/me/switch-tenant/csrf handlers; login always issues a brand-new session (fixation defence) and switch-tenant rotates the existing session's token; a decoy-hash comparison on unknown-email login for timing-safe enumeration resistance.
- **CSRF** (`api/internal/csrf`): synchronizer token (hash stored on the session row), `X-CSRF-Token` header, Origin/Referer allowlist.
- **RBAC** (`api/internal/rbac`): reads `x-min-role` from the bundled OpenAPI spec (`gen.GetSwagger()`) into a `StrictMiddlewareFunc`; `BuildMinRoles` fails at startup if any operation lacks `x-min-role` (deny-by-default) or uses an unrecognised value.
- **Rate limiting**: a Postgres token bucket (`api/internal/ratelimit.DBBucket`, two statements per call — see deviation below) for login (5/min per IP+email, 20/hour per IP), and an in-memory token bucket for general flood defence (100/min per client IP, trusted-proxy-aware `httpx.RealIP` since chi's own `middleware.RealIP` is deprecated/spoofable).
- **Audit** (`api/internal/audit`): `Record` writes in the same transaction as the action it describes; `audit_log` has a `BEFORE UPDATE OR DELETE OR TRUNCATE` trigger that raises even for the table owner, and `loomtale_app` is revoked `UPDATE`/`DELETE`/`TRUNCATE` (INSERT/SELECT only); `GET /audit` is owner-only, cursor-paginated.
- **Storage** (`api/internal/storage`): `Internal` (in-network, full app creds, `GetObject`/Range, `PutObject`, internal presigned GET) and `Browser` (signs against `MINIO_PUBLIC_ENDPOINT`, presigned POST pinned to the exact key via `SetKey`, refuses any key under `t/{tenant}/render/`, ≤10 min TTL). `Finalize` does a HEAD (checksum, size) plus a 512B range-GET magic-byte sniff — never streams the full object — records `sha256` from an `x-amz-checksum-sha256` upload trailer when present, and validates the sniffed MIME against a per-kind allowlist (`document` trusts the presign-time declared MIME instead, since text/* has no magic bytes).
- **Request validation & error mapping**: `api/internal/validation` mounts a `kin-openapi`/`openapi3filter` validator built from the same bundled spec; `cmd/api/main.go` wires a problem+json `ResponseErrorHandlerFunc`/`RequestErrorHandlerFunc` that logs the real error server-side and returns a fixed, non-leaking title to the client.
- **Headers & logging**: `api/internal/secheaders` builds the exact enforcing CSP from the plan (no Report-Only), shared byte-for-byte with `deploy/caddy/Caddyfile` via `{$MEDIA_ORIGIN}`; the Caddyfile now uses `handle` blocks so `/api/*` responses carry only the API's own headers and static SPA responses carry only Caddy's (the previous `route`+global `header` shape stacked both sets on every response). `api/internal/obs/scrub` redacts secret-shaped log keys and strips capability-URL query strings (`X-Amz-Signature`, `X-Amz-Credential`, `upload_id`, `upload_protocol`); `api/internal/secretstr.String` redacts through `String()`/`GoString()`/`slog.LogValue()`/`MarshalJSON()`.
- **`loomtale create-owner`**: password read from stdin (TTY prompt or piped line), never argv; creates tenant + user + owner membership in one transaction.
- **Backups**: `deploy/backup/backup.sh` — `pg_dump -Fc`, age-encrypts (identity from the `backup_encryption_key` secret, recipient derived via `age-keygen -y`), uploads to `$BACKUP_TARGET` via `mc`; mirrors the object bucket locally, age-encrypts each object, uploads the encrypted mirror; retention prunes to 7 daily + 4 weekly dumps; records outcome in `backup_runs`. `deploy/docker/backup.Dockerfile` (Alpine 3.21, `postgresql17-client` to match the Postgres 17 server, `age`, `dcron`, `mc` copied from the same pinned `bitnamilegacy/minio-client` image `minio-init.sh` already uses). `/readyz` surfaces backup freshness (`api/internal/ops.BackupStatus`) as a `checks.backup` detail, never gating readiness itself.
- **Compose wiring**: new secrets (`postgres_owner_password`, `postgres_app_password`, `master_key`, `backup_encryption_key`, `backup_target_credentials`); a `migrate` one-shot service (same `api` image, `loomtale migrate up`, owner DSN) gating `api`/`worker` via `depends_on: service_completed_successfully`; Postgres gets `wal_compression=on` (`max_wal_size=2GB` already present); MinIO app credentials come from `.env` (`MINIO_APP_ACCESS_KEY`/`MINIO_APP_SECRET_KEY`, operator-provisioned once, never generated/printed at runtime) so `minio-init.sh` and the `api` service always agree — this is the fix for the phase 1 follow-up ("`/readyz` 503 until the MinIO app key is wired manually"). `scripts/dev-secrets-init.sh` generates every secret + matching `.env` value (including a real `age-keygen` identity) in one command for local dev.

## Commands run — pass/fail

All via the toolbox (`COMPOSE_PROJECT_NAME=loomtale-p2`) unless noted.

| Command | Result |
|---|---|
| `go build ./...` (api, tools) | pass |
| `go vet ./...` (api, tools) | pass |
| `golangci-lint run ./...` (api, tools) | pass, 0 issues |
| `go test ./... -race -count=1` (api) | pass, all packages |
| `go test ./... -race -count=1` (tools) | pass |
| `uv run ruff check .` / `uv run pytest -q` (workers-python) | pass (untouched by this phase) |
| `npm run lint` / `npm run typecheck` (web) | pass |
| `cd api && ./bin/tenantctx ./...` | pass, 0 violations against the real codebase |
| `bash scripts/lint-tenant-queries.sh` | pass (`OK`) |
| `go tool govulncheck ./...` | pass, 0 vulnerabilities (found and fixed a real one — see deviations) |
| `npm audit --audit-level=high` (web) | pass, 0 vulnerabilities |
| `make gen` then `git diff` (host-side, since `gen-check`'s own `git` call fails inside the toolbox in this worktree, as flagged in the task) | pass, zero diff after commit |
| `docker compose -p loomtale-p2 -f deploy/compose.yml --env-file .env up -d --wait` (all 7 services) | pass, every service healthy, zero manual steps |
| `curl /api/v1/healthz`, `/readyz` | 200, `{"status":"ok",...}` both through the API directly and through Caddy |
| Integration suite (`go test -tags=integration ./internal/integration/...`, run via `docker run` attached to `loomtale-p2_loomtale_core` since `docker compose run` cannot join an external network) | pass, 13/13 |
| Manual backup exercise (see deviations — full run not possible without a live cloud target) | `pg_dump` (PG17 client) → `age -r`/`age -d` roundtrip verified byte-identical; `mc` confirmed reachable/working via `minio-init.sh`'s own use every stack boot |

Integration tests (`api/internal/integration`, build-tagged `integration`, not wired into `.github/workflows/ci.yml` — matching the existing `workerpb` integration test's pattern, so this phase does not change CI):
`TestAuditLogRejectsUpdateAsOwnerRole`, `TestAuditLogRejectsUpdateAsAppRole`, `TestAuditLogRejectsDeleteAsOwnerRole`, `TestAppRoleHasNoDDLRights`, `TestOwnerLoginAndMe`, `TestViewerGetsForbiddenOnEditorRoute`, `TestLoginRejectsWrongPassword`, `TestUnauthenticatedRequestIsRejected`, `TestCSRFRejectsRequestMissingToken`, `TestCrossTenantAssetAccessReturns404`, `TestSwitchTenantRequiresMembership`, `TestPresignRefusesRenderPrefixAndBadMIME`, `TestFinalizeWithoutUploadIsRejected` — all pass against the live compose stack.

## Perf numbers

- Login p95: 5 samples, 58–120ms (`curl -w %{time_total}`), well under the 400ms budget.
- Authenticated-read middleware overhead: `/healthz` (no session/tenant/RBAC) vs `/auth/me` (full chain incl. session lookup, membership lookup, RBAC) — both 2.6–3.2ms total request time; the marginal overhead is within measurement noise, not a distinguishable few ms, well under the ≤3ms budget.
- `EXPLAIN` on the assets cursor query (`tenant_id = $1 AND id > $2 ORDER BY id LIMIT $3`) shows `Index Scan using assets_tenant_id_id_idx`, confirmed for the audit query the same way (same `(tenant_id, id)` index shape).
- No API handler reads S3 streams except finalize's 512B range GET (`storage.Internal.Finalize`) — verified by code review of every `assetsapi` handler; `GetAsset`/`ListAssets` only ever call `Browser.PresignGet`/DB queries, never `GetObject`.

## Security checklist — verified mechanically

- [x] Cross-tenant access returns 404: `TestCrossTenantAssetAccessReturns404` (GET and finalize both), also manually verified via curl with two real tenants.
- [x] Tenant never read from request params: `tenantctx` analyzer runs clean against the real codebase in `make lint`.
- [x] Session fixation: login always creates a new session row (never reuses/updates an existing one); switch-tenant rotates token+CSRF hash on the existing row. Logout deletes the row (verified: `sessions` table empty after logout in manual testing).
- [x] CSRF enforced on every unsafe method; Origin checked: `TestCSRFRejectsRequestMissingToken`, plus manual curl (missing token → 403, bad Origin → 403 in `csrf` unit tests).
- [x] Secrets/capability URLs never in logs: `obs/scrub` and `secretstr` unit tests assert redaction through `slog` JSON output for both attribute-key matching and URL query-string stripping.
- [x] Browser presigned URLs ≤10min, exact key pinned (`SetKey`, not a prefix), `render/` refused (`ErrRenderPrefixForbidden`, unit-tested indirectly via `IsRenderKey`/`Key` tests and the presign handler's call site); internal URLs use step-scoped TTL and are never returned to a browser-facing response type.
- [x] Upload MIME sniffed server-side, declared type not trusted for finalize (except the documented `document`-kind exception, since text/* has no magic bytes — the declared MIME is still allowlist-checked at presign time): `sniff_test.go` plus `TestFinalizeWithoutUploadIsRejected`/manual finalize test showing the sniffed `image/png` overriding nothing incorrectly.
- [x] `audit_log`: UPDATE/DELETE/TRUNCATE raise even as table owner (`TestAuditLogRejectsUpdateAsOwnerRole`, `...DeleteAsOwnerRole`, plus a manual `psql` `UPDATE` as the bootstrap superuser); app role has INSERT/SELECT only (`TestAuditLogRejectsUpdateAsAppRole`, manual `psql` `permission denied` before even reaching the trigger); down migration refuses (`RAISE EXCEPTION` in `20260925010500_audit.sql`, code-reviewed — goose down was not actually invoked against a live DB since that would need a second migration to roll back first).
- [x] KEK absent → process refuses to start: `envelope.LoadKEK` returns an error `run()` propagates before the HTTP server ever listens; exercised implicitly every compose boot (the `master_key` secret is mounted, and removing it was not separately re-tested against a live container but is a direct, unconditional `os.ReadFile` failure path).
- [x] Enforcing CSP only, no Report-Only, `object-src 'none'`, Trusted Types: `secheaders` unit tests plus manual curl against both the API directly and through Caddy (`Content-Security-Policy` present once, byte-identical on both, no `Content-Security-Policy-Report-Only` header, `trusted-types default dompurify` and `require-trusted-types-for 'script'` present).
- [x] `loomtale_app` has no DDL rights: `TestAppRoleHasNoDDLRights` (`DROP TABLE users` → `permission denied`) plus manual `psql` confirmation.

## Deviations, with reasons

- **RT#8's browser-presign cache-flooring** (signing time floored to a 5-minute window for byte-identical URLs) is not implemented: `minio-go`'s `Client.Presign`/`PresignedGetObject` always sign at `time.Now()` with no caller-supplied clock, so achieving this needs a hand-rolled SigV4 signer. Documented in `storage/browser.go`'s doc comment as a follow-up rather than faked; it is a caching nicety, not a security or success-criteria item.
- **`ConsumeRateLimitToken` was redesigned from one CTE-chained statement into two statements** (`RefillRateLimitBucket` + `ConsumeRateLimitBucket`). The original single-statement design (an `INSERT ... ON CONFLICT ... RETURNING` CTE feeding a second write to the same table) is unsound in Postgres: sibling data-modifying CTEs targeting the same table execute against the same pre-statement MVCC snapshot and cannot see each other's writes (documented in Postgres's own "WITH Queries" chapter), which I hit as a real bug during manual testing (a brand-new bucket key's very first request was either always denied or errored "no rows in result set" depending on which broken variant I tried) before landing on the two-statement fix, which I verified live against the real stack (login succeeded, no false 429/500). This trades a small race window under heavy concurrent load on the exact same bucket key for correctness on every other case; acceptable for a login rate limiter.
- **`github.com/getkin/kin-openapi` bumped 0.142.0 → 0.144.0**: `govulncheck` found GO-2026-6112 (nil-pointer panic in `openapi3filter.ValidateRequest`, which `validation.Middleware` calls directly) reachable from this phase's own new code. Fixed by upgrading; `oapi-codegen` (the only other consumer) still builds and generates identically against the newer version.
- **`golang.org/x/term` and `github.com/oapi-codegen/runtime` added as new direct dependencies** (`create-owner`'s stdin password prompt; oapi-codegen v2.8.0's generated code needs a newer `runtime` than the `v1.1.1` the toolchain initially resolved — bumped to `v1.7.0`).
- **DB role passwords are real Docker secrets, not fixed/committed values.** I considered simplifying to a fixed dev-only password (to sidestep the two-places-to-set-the-same-value friction — `.env` for the app/migrate/worker/backup DSNs, `secrets/*.txt` for `init-roles.sh`) after worrying it would break CI, but confirmed `.github/workflows/ci.yml` never runs anything that applies these migrations (`test-integration` isn't a CI step, matching the pre-existing `workerpb` integration test's status), so there is no CI-compatibility reason to weaken it. Kept the real-secrets design since it is already implemented, tested end-to-end, and is the more correct shape for anything beyond pure local dev. `scripts/dev-secrets-init.sh` removes the operator friction for local dev by generating and syncing both places in one command.
- **`tools/tenantctx` lives in its own Go module** (`tools/go.mod`), not under `api/`, because the plan's file list places it at repo-root `tools/tenantctx/` while `api/` is its own module; `make lint` builds the analyzer binary from the `tools` module and runs it with its working directory inside `api/` (`cd api && ./bin/tenantctx ./...`) so package-pattern resolution uses `api`'s own module regardless of where the analyzer binary itself was built — the standard way to run a cross-module `go/analysis` tool.
- **Backup's cloud upload step (`mc mirror`/`mc cp` to `$BACKUP_TARGET`) was not run against a real Cloudflare R2/Backblaze B2 bucket.** No such account/credentials exist in this environment, and writing a target-credentials secrets file even with locally-generated placeholder values triggered this session's privacy-block hook, which requires interactive user approval via a tool (`AskUserQuestion`) that is unavailable to a subagent. What I did verify directly against the live containers: `pg_dump` against the real Postgres 17 server (fixed a client-version mismatch — Alpine 3.20's default `postgresql-client` resolves to v16, which refuses to dump a v17 server; switched to Alpine 3.21 + `postgresql17-client`), `age` encrypt/decrypt round-trip (byte-identical via `cmp`), and MinIO's S3 API via `mc` (exercised indirectly every stack boot through `minio-init.sh`, which the same pinned `mc` binary powers). I also found and fixed two container-stability bugs while getting this far: the `backup` service's `tmpfs`-mounted `/var/spool/cron` shadows the image-built `crontabs/` subdirectory at runtime (entrypoint now `mkdir -p`s it), and `crond -f` (foreground) fails `setpgid` as PID 1 under this container's capability restrictions (switched to `crond` daemonizing normally, backed by `/var/run` also on `tmpfs` for its pidfile, plus `exec tail -f /dev/null` to keep the container alive).
- **`age`'s dev-only encryption identity is generated correctly, but only after an initial mistake**: `scripts/dev-secrets-init.sh` originally generated `backup_encryption_key.txt` as base64 random bytes (matching the `master_key` pattern), which is not a valid age identity. Fixed to shell out to `age-keygen` (run once inside the already-built `backup` image, since `age-keygen` isn't on the host or in the toolbox), verified live.
- **Trailing `X-Frame-Options`/legacy header dropped** from the Caddyfile in favour of the CSP's `frame-ancestors 'none'`, which supersedes it in every modern browser; not in the plan's explicit header list, and keeping it would have meant the API and Caddy sending different header sets again (the API's `secheaders` package never set it).
- **`AuditEntry.metadata`** (optional `additionalProperties` object) is populated from the JSONB column when non-empty; not explicitly required by the spec but trivial and consistent with the schema.

## Review fixes

Source: `plans/reports/code-reviewer-260925-1203-phase-02-security-review.md`. All High and Medium findings addressed; Low findings fixed where cheap, rest deferred with reasons. Migrations `db_roles.sql`/`core_tenancy.sql`/`assets.sql`/`ops.sql`/`audit.sql` edited in place (phase still unmerged, per coordinator decision), not new migrations.

### High

- **H1 (pre-auth argon2 OOM DoS).** Fix: `auth.HashLimiter` (semaphore, default 4, `ARGON2_MAX_CONCURRENCY`) wraps both the real `VerifyPassword` call and the constant-time decoy in `Login`; 429 when full. `GOMEMLIMIT=400MiB` set on the api container (512m cgroup limit). Evidence: `api/internal/auth/argon2_limiter.go`; `docker compose ... config` shows `GOMEMLIMIT` in the api service; login still completes in ~120–230ms in the live-stack integration run (no regression).
- **H2 (no request body size limit).** Fix: `httpx.MaxBodyMiddleware` (`http.MaxBytesReader`, 256KiB) added globally ahead of the validator; Caddy `request_body { max_size 256KB }` on `/api/*`; `maxLength: 320`/`1024` added to `LoginRequest.email`/`password`. Evidence: `api/internal/httpx/body_limit.go`, `deploy/caddy/Caddyfile`, `openapi/schemas/auth.yaml`.
- **H3 (hardcoded superuser password).** Fix: `POSTGRES_PASSWORD_FILE=/run/secrets/postgres_superuser_password`, new `postgres_superuser_password.txt` secret generated by `dev-secrets-init.sh`; `POSTGRES_PASSWORD=change-me` dropped from `.env.example`; no `.env` var for it at all (file-only). Evidence: `deploy/compose.yml`'s `postgres` service; fresh stack boots and passes `pg_isready`/full test suite with this in place.
- **H4 (audit trigger blocks tenant/user DELETE).** Decision applied: dropped the `REFERENCES tenants/users ... ON DELETE SET NULL` FKs on `audit_log.tenant_id`/`actor_user_id`; both are now plain `uuid` columns (indexed), plus a new denormalized `actor_email` so entries stay readable after the user row is gone. Edited `20260925010500_audit.sql` in place. Evidence: schema diff in the migration; `TestAuditLogRejects{Update,Delete,Truncate}As{Owner,App}Role` still pass (immutability itself untouched, only the FK removed).
- **H5 (backup breaks at scale / unbounded growth).** Fix: rewrote `backup.sh` to stream `pg_dump -Fc | age | mc pipe` directly (no local plaintext dump) and to sync the object bucket **object-by-object** the same way, skipping any object already present at the target (assets are immutable once finalized, so existence is a correct incremental-sync signal) — no more full-bucket nightly re-copy. Dump retention (7 daily/4 weekly) kept; object "retention" reinterpreted as "the live bucket's own content", not point-in-time snapshots (documented in the script). Evidence: manual run against the real stack — `docker exec loomtale-p2-backup-1 /backup.sh` → `[backup ...] OK`, `mc ls target/loomtale/postgres/` shows the 25KiB encrypted dump, `/readyz` flips `backup` from `"no backup run yet"` to `"ok"`.
- **H6 (XFF trust too broad).** Fix: `loomtale_core` network pinned to `172.28.0.0/24`; `web` (Caddy) given a fixed `172.28.0.10`; api's `TRUSTED_PROXY_CIDRS=172.28.0.10/32` (was RFC1918-wide by default). Evidence: `deploy/compose.yml`; `httpx.RealIP` unit tests unchanged/still pass since the default is only a fallback now.

### Medium

- **M1 (viewer skips tenant check).** Fix: new `RoleAuthenticated` tier below `RoleViewer` in `rbac.Role`; `me`/`csrf`/`logout`/`switchTenant` now `x-min-role: authenticated` (session only); `viewer` and above always require a resolved `tenant.Info` or 403. Evidence: `TestMiddlewareAuthenticatedTierAllowsSessionWithNoTenant`, `TestMiddlewareViewerTierRequiresTenant` (new, pass).
- **M2 (switch-tenant drops the new CSRF token).** Fix: `switchTenant`'s 200 response reuses `LoginResponse` (`{me, csrfToken}`) instead of bare `Me`. Evidence: `openapi/paths/auth.yaml`; `TestOldCookieRejectedAfterSwitchTenant` exercises the new response shape end-to-end.
- **M3 (GET /auth/csrf is a GET with a write side effect).** Fix: CSRF token is now `HMAC-SHA256(pepper, "csrf|"+sessionToken)` — deterministic, never stored, never rotated; `csrf_token_hash` column dropped from `sessions`. `GetCsrf` is now a pure computation. Pepper reuses the loaded KEK bytes with domain separation (`"csrf|"` prefix), no new secret. Evidence: `api/internal/csrf/csrf.go`, `core_tenancy.sql`; `csrf_test.go` rewritten for `Derive`/`Verify`, all pass.
- **M4 (rotate resets expires_at).** Fix: `RotateSession` no longer touches `expires_at`. Evidence: `db/queries/auth.sql`'s doc comment + query; `TestOldCookieRejectedAfterSwitchTenant` confirms rotation still works without asserting the (unchanged-by-design) expiry.
- **M5 (scrub gaps).** Fix: case-insensitive param match (`x-amz-signature` lowercase now caught); URLs embedded anywhere in a larger string are scrubbed (regex extraction, not "whole string is a URL"); `slog.KindAny` values that implement `error` are scrubbed via `err.Error()`. Evidence: `api/internal/obs/scrub/scrub.go`; new tests `TestURLScrubsEmbeddedInLargerText`, `TestURLMatchesParamsCaseInsensitively`, `TestReplaceAttrScrubsErrorTypedValues`, all pass.
- **M6 (re-upload after finalize bypasses the sniff).** Fix: bucket versioning enabled (`mc version enable`); `Finalize` captures `info.VersionID`, stored as `assets.storage_version_id`; every download URL (`storage.Internal`/`Browser.PresignGet`) pins that exact version via a `versionId` query param. Evidence: `assets.sql` migration, `storage/{client,browser,finalize}.go`; **manually verified live** (Go-test automation of this needs host Go, unavailable in this sandbox — see below): presigned upload → finalize → re-upload tampered bytes to the same key → the original `downloadUrl` still serves the byte-identical original content (`cmp` exit 0 against original, exit 1 against tampered).
- **M7 (audit immutability covers app role only; backup holds owner+root creds).** Fix: new `loomtale_backup` role (SELECT-only, plus a narrow `INSERT, UPDATE` grant on `backup_runs` for its own bookkeeping) and a new read-only (`GetObject`/`ListBucket`) MinIO key (`loomtale-backup`) for the sidecar; backup no longer connects as `loomtale_owner` or holds MinIO root credentials. Evidence: `db_roles.sql`, `minio-init.sh`, `compose.yml`; `TestBackupRoleIsReadOnly` (new) confirms INSERT on `tenants` fails; live backup run using this exact role succeeded.
- **M8 (backup mounts the private age identity; write-only-key prune contradiction).** Fix: only the derived public recipient (`BACKUP_ENCRYPTION_RECIPIENT`, not sensitive) is passed to the backup container; the identity file is generated once by `dev-secrets-init.sh` and kept in `secrets/` for the operator to move offline — never mounted into any container (confirmed live: `age-keygen -y /run/secrets/backup_encryption_key` inside the running backup container fails with "no such file"). The "write-only key can't prune" contradiction is resolved by design: target-bucket retention (`mc rm` on old dumps) still needs delete rights on the *target*, documented as an operator requirement for the target key (separate from the read-only *local* MinIO key M7 fixes); this is unchanged from the original design intent and was already correctly scoped to the target credentials, not the local ones.
- **M9 (tenant-query lint/analyzer gaps).** Fixed: `lint-tenant-queries.sh` rewritten (correct comment-to-block attribution via awk instead of csplit, `JOIN` detection, comments stripped before matching, `tenant_quotas` added to the tenant-table list, filter must be an `AND`/`WHERE` clause not just present anywhere). `tenantctx` analyzer now tracks single-hop local-variable taint (`id := req.Body.TenantId; ...; X{TenantID: id}` is now flagged) and recognises an allow-comment covering a whole comment-group, not just the exact adjacent line. Found and fixed a real over-tainting bug during this work (see "Issues found" below). Evidence: `scripts/lint-tenant-queries.sh`, `tools/tenantctx/analyzer.go` + expanded `testdata/src/a/a.go`; `make lint` passes clean against the real codebase (0 flags on legitimate code, 2 documented `tenantctx:allow` exceptions in `switch_tenant.go`).
- **M10 (validator fails open).** Fix: a `FindRoute` miss now returns 404 (fail closed) instead of proceeding to the handler; validation errors are logged server-side and a generic detail returned to the client (folds in L5 too). Evidence: `api/internal/validation/middleware.go`.
- **M11 (AAD bound to mutable id).** Fix: `envelope.AAD`'s third parameter is now explicitly documented/named for `owner_ref` (the natural key `UpsertSecret` upserts on), not a record id; doc comment explains why binding to a surrogate id breaks on upsert. No behavior change needed beyond the contract clarification since there is still no caller in this phase. Evidence: `api/internal/crypto/envelope/envelope.go`.
- **M12 (integration tests: happy-path only, not in CI, ambiguous 403s).** Fixed: `CI=true` (GitHub Actions sets this) makes `missingEnv`/`skipIfAPIUnreachable` `t.Fatal` instead of `t.Skip`; new dedicated `integration` CI job brings up the full compose stack (`deploy/compose.ci.yml` publishes Postgres for the runner-hosted `go test`) and runs the suite; `requireProblem` helper asserts the problem+json `title`, disambiguating CSRF's 403 ("CSRF check failed") from RBAC's ("insufficient role for this tenant") — `TestViewerGetsForbiddenOnEditorRoute` and the CSRF tests now assert their specific title. Added: old-cookie-401-after-logout, old-cookie-401-after-switch-tenant, wrong/missing CSRF token, bad Origin, TRUNCATE (both roles), DELETE-as-app-role, list isolation (`listAssets`, `listAudit`), rate-limit 429, upload/MIME-mismatch rejection, re-upload-after-finalize (version pinning). Deferred: a "planted secret in captured log output" scan — the integration harness has no access to the containers' own stdout/log stream (would need a log-shipping or `docker logs`-scraping harness this phase doesn't build); `obs/scrub` unit tests already cover the scrubbing logic itself directly. Evidence: `.github/workflows/ci.yml`'s new `integration` job; `api/internal/integration/*_test.go`; 21/23 tests pass live (2 need host Go, see below), full suite compiles and vets clean with `-tags=integration`.

### Low (fixed)

- **L3** (init-roles.sh interpolates secrets into raw SQL): rewritten to use `psql -v ... :'var'` substitution (proper SQL-literal quoting) instead of shell string interpolation. Found and fixed a real bug in the process: `:'var'` substitution does not work inside a `DO $$ ... $$` block (psql skips it there), so the create-or-alter logic was restructured with `\if`/`\gset`/`\endif` around plain top-level statements instead of a PL/pgSQL DO block.
- **L4** (secrets written 0644): `umask 077` added to `dev-secrets-init.sh` (verified in the script; not independently checkable from this Windows/MSYS sandbox, whose filesystem does not surface POSIX modes the same way — the fix is real and takes effect on the Linux hosts (CI, deployment) this actually matters for).
- **L1** (DB errors swallowed as "no session"): `Store.Lookup` now returns genuine errors instead of always `(false, nil)`; `auth.Middleware` returns 500 on a real error instead of silently falling through to unauthenticated.
- **L2** (partial): per-user bucket key now lowercases the email; added a third, IP-independent `LoginPerAccount` bucket (10/hour by email alone) closing the "spread attempts across many IPs" gap; added best-effort `DeleteExpiredSessions`/`DeleteStaleRateLimitBuckets` cleanup piggybacked on every login.
- **L7** (`gcm.Open` panics on a bad nonce length): explicit length check added before calling `Open`, returning an error instead of crashing the process on a corrupt row.
- **L8** (partial): `Store.Create` now deletes all of a user's existing sessions before creating the new one (re-login revokes stale sessions); failed-login audit entries redact the attempted "email" to `[REDACTED: ...]` when it doesn't look like an email (catches a password mistyped into the email field).
- **L10**: replaced chi's plain-text `middleware.Logger` with `httpx.AccessLogMiddleware` (structured slog, goes through the same scrubbing `ReplaceAttr`); `X-Request-Id` now validated as a UUID before being echoed/logged, a non-UUID client value is replaced instead of trusted.

### Low (deferred, with reasons)

- **L6** (no HSTS on Caddy's own SPA responses): tried a `{$HSTS_HEADER_VALUE}` placeholder approach and reverted it — an empty placeholder still emits an (invalid, but present) header, and Caddy has no clean way to conditionally omit a header line based on `PUBLIC_URL`'s scheme without either a second env var an operator must keep in sync by hand or a CEL matcher expression not confirmed available in the pinned Caddy version. Real production HTTPS termination at Caddy (per the coordinator's decision) needs its own site-block config (automatic HTTPS, which adds HSTS itself) that this phase's local-dev-oriented `:8080` Caddyfile doesn't attempt; deferred to whichever phase builds that.
- **L9** (browser uploads usually have no sha256; `document` content never inspected; `ftyp` sniff accepts HEIC/MOV as `video/mp4`): unchanged. The `x-amz-checksum-sha256` mechanism this relies on needs SPA-side `SubtleCrypto` hashing before upload, which is phase 5's territory, not phase 2's Go backend; `document` kind's lack of magic bytes is inherent to plain text and already documented as a deliberate trust-the-declared-MIME exception; the `ftyp` box sniff being too coarse (HEIC/MOV vs MP4) is a real gap but a narrow one (both are still legitimate media containers, not an arbitrary-file-type confusion) better fixed together with L9's SubtleCrypto work in the frontend phase.

### Verification notes / issues found while fixing

- **Two real product/tooling bugs found and fixed during this work, independent of the original findings**: (1) the original `ConsumeRateLimitToken` single-CTE design (already shipped) turned out to have a genuine Postgres read-your-own-write bug — sibling writable CTEs never see each other's writes within one statement — surfaced again while re-verifying H1's rate limiting; already split into two statements in the original implementation, reconfirmed still correct here. (2) `db_roles.sql`'s new `loomtale_backup` role needed two more grants discovered only by actually running `pg_dump` as that role: `goose_db_version` and its sequence predate the default-privilege grant (goose creates them before any of this phase's migrations run) and needed explicit `GRANT SELECT`.
- **A real config bug was introduced and caught during manual verification, not by any automated check**: the H5 backup.sh rewrite dropped the `BACKUP_TARGET` environment variable from `compose.yml` entirely (present in `backup.sh`'s required-var check, absent from the service definition) — caught only by actually running `docker compose config` and `docker exec ... /backup.sh` against a live stack. Fixed, and a new `BACKUP_TARGET_BUCKET` var added (the rewritten object-sync logic needs a bucket name, not just an endpoint, to build target paths under).
- **Full re-verification performed**: fresh `docker compose -p loomtale-p2 up -d --wait --build` (zero manual steps, all 7 services healthy), `scripts/tb.sh ci` (lint incl. `tenantctx` + `lint-tenant-queries.sh`, unit tests incl. `-race`, `govulncheck`, `npm audit`) all green, `make gen` idempotent post-commit (gen-check's own git call still fails inside the toolbox in this worktree, as previously documented — verified via host-side `git status`/`diff` instead), 21/23 integration tests pass live against the fresh stack.
- **2 integration tests need host Go to run** (`TestFinalizeRejectsUploadWithMismatchedMagicBytes`, `TestUploadFinalizeAndReuploadIsIsolatedByVersion`): both upload through the *browser*-facing presigned URL (`127.0.0.1:9000`), which is only reachable from an actual host network stack, not from a container attached to the compose network (this sandbox has no host Go — see original constraints). The new CI `integration` job runs `go test` directly on the GitHub Actions runner (host-level, same machine as the Docker daemon), where both will run for real. Manually verified the exact mechanism both tests check via `curl` from this session's host-reachable shell: presign → upload → finalize (200, `status: ready`) → re-upload tampered bytes to the same key (204, new version created) → original `downloadUrl` still returns the byte-identical original content; and a genuinely non-image upload → finalize returns 422 "mime sniff mismatch".
- **Verified `127.0.0.1` as a secure context** (coordinator's open question): Chrome and Firefox both treat `127.0.0.1`/`localhost` as "potentially trustworthy origins" per the W3C Secure Contexts spec (confirmed via web search plus this session's own successful `__Host-`-cookie round trips over `http://127.0.0.1:8080` throughout testing) — `WEB_ORIGIN`/compose ports left as `127.0.0.1`, no change needed.

## Unresolved questions

1. The full nightly-backup cloud upload path against a **real** R2/B2 endpoint (as opposed to the local MinIO instance used as a stand-in target for this round's manual verification) is still unverified — should the controller supply a real (even short-lived, revocable) R2/B2 test bucket to close this out before merge, or defer to phase 12's restore drill?
2. `db_roles.sql`'s design still requires the operator to keep `.env`'s `POSTGRES_*_PASSWORD` vars and `secrets/postgres_*_password.txt` in sync (two places, one value each) — `scripts/dev-secrets-init.sh` automates this for local dev; a real deployment's secret-rotation story is out of scope here and worth a `docs/deployment-guide.md` note in a later phase.
3. `goose down` on the audit migration is still verified only by code review (unchanged from the original report), not exercised live.
4. L9's `document`/HEIC-MOV sniff gaps and L6's HSTS-on-Caddy gap are deferred as above — worth tracking so they aren't lost before the phases that naturally pick them up (5 and whichever phase adds production HTTPS termination, respectively).
