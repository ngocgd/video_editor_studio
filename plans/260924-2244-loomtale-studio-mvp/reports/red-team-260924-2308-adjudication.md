# Red Team Adjudication — Loomtale Studio MVP (2026-09-24)

Reviewers: Security Adversary (10), Failure Mode Analyst (10), Assumption Destroyer (9), Scope & Complexity Critic (10). 39 raw findings, deduplicated into 15. All passed the evidence filter (plan file:line citations; host facts verified read-only).

| # | Finding (merged sources) | Sev | Disposition | Applies to |
|---|---|---|---|---|
| 1 | Job engine: River rescue (15m) < job timeouts (2–3h) → live jobs re-run; batch-level `Attempt` cannot fence per-step; two rescue mechanisms racing. Fix: River `RescueStuckJobsAfter` > longest timeout; DB CAS per step (`UPDATE … SET attempt=attempt+1,status='running' WHERE status='queued' RETURNING`) is the fence; job args = step IDs only; handlers skip non-queued steps; reconciler regroups by (stage, model). (FM1, FM6, SC1, SC2) | Critical | Accept | P3 |
| 2 | Fan-in lost-wakeup race (compose never/twice enqueued); SSE LISTEN loss freezes UI; quota hook promised but never implemented; SSE per-session cap vs multi-tab. Fix: `remaining_deps` atomic counter + unique enqueue + reconciler sweeps ready `pending`; SSE `resync` event, subscribe-before-snapshot, per-step `version`, terminal events never coalesced, per-user cap / BroadcastChannel; `quota.Check` in `pipeline.Enqueue` (unlimited local default). (FM2, FM9, SC8, SC9-part) | Critical | Accept | P3, P5 |
| 3 | YouTube upload not idempotent (crash after final chunk → duplicate public video); custom thumbnails need a verified channel; quota numbers unverified; CTR is only in the Reporting API. Fix: persist session URI before first byte, handle 200/201/404/410, metadata nonce + uploads-playlist dedupe, publication CAS `approved→uploading`; channel eligibility precheck, 403 thumbnail = permanent; conservative 1600-unit ledger until live-verified; CTR per user decision. (FM3, AD8, AD3) | Critical | Accept | P10, P11 |
| 4 | Docker/WSL VM has 8GB RAM / 4 CPUs (`.wslconfig`), not 32GB; no RAM budget. Fix: phase-1 host step raising `.wslconfig` (user decision), per-service `mem_limit` table, GGUF Q4 for Qwen-Image(-Edit), go/no-go gate. (AD1) | Critical | Accept | P1, P9 |
| 5 | VRAM: desktop already uses ~5.4GB (≈10.9GB free); NVENC/CUDA filters outside the slot; async unloads; OOM retried identically; trainer outside residency; nvidia-smi can't prove per-process residency on WDDM. Fix: budget from measured free VRAM; `Ensure` polls `GpuProbe` until free; render admitted only with VRAM reserve, no CUDA filters while a model is resident; OOM class = full unload + 1 retry; ai-toolkit as a pyworker engine; residency proven app-side (ComfyUI `/system_stats`, Ollama `/api/ps`, pyworker `ListEngines`). (AD2, AD9, FM5, SC5, SC6) | Critical | Accept | P3, P4, P8, P9 |
| 6 | Static `Queue()` cannot follow runtime provider (Ollama must use `gpu`); interactive LLM starved behind 2h batches; advisory lock silently lost. Fix: `Queue(ctx, StepRef)` resolved at enqueue; priorities (interactive 1, per-scene 2, batch 3, train/bench 4); ~10-min batch chunks; prefer resident model; `pg_try_advisory_lock` + snooze; lock-connection watchdog cancels ctx. (SC3, FM10) | High | Accept | P3, P4, P6 |
| 7 | Plan silently demoted Ollama from the user's local default to "optional". Fix: Ollama required service, seeded default after phase 9; Ollama variant of phase-6/7 success criteria. (SC4) | High | Accept | P6, P9, plan.md |
| 8 | 10-min presigned URLs used by multi-hour FFmpeg/upload streams; one signing client for browser vs in-network hosts. Fix: workers use direct minio-go credentials/internal presign with step-length TTL; per-chunk range GET for upload; browser presign keeps ≤10 min with its own endpoint. (FM4, SEC-Q4) | High | Accept | P2, P8, P10 |
| 9 | No input snapshot for a render run → mixed old/new takes, A/V drift. Fix: freeze a render manifest at run creation; compose hash = manifest hash; edit during render per user decision. (FM7) | High | Accept | P7, P8 |
| 10 | No DB/object backups; disk budget ignores Docker images (~53GB used), VHD growth, render assets. Fix: nightly `pg_dump` + `mc mirror` to a non-C: target with restore drill; disk watermark guard on enqueue; one Qwen-Image-Edit revision; segment/take TTL cleanup; `max_wal_size`. (FM8, AD4) | High | Accept | P2, P8, P9, P12 |
| 11 | claude CLI shares the worker container with KEK/DB/YouTube secrets, no sandbox; shell-notation argv; credentials.json fallback risks host logout. Fix: dedicated `llm-cli` sidecar with only its token, non-root, read-only rootfs, tmpfs HOME, egress only to Anthropic; argv as Go `[]string`; assert init `tools: []`; drop credentials.json fallback. (SEC1, AD6) | High | Accept | P4 |
| 12 | Injected text becomes trusted after derivation (summaries/bible in system prompt); LLM-emitted character UUIDs trusted; public YouTube metadata exposed. Fix: `origin/tainted` provenance on derived content; all story content in nonce-fenced data blocks; map characters by name server-side; confirm URLs in suggested metadata; canary-based injection tests. (SEC2, SEC10-part) | High | Accept | P4, P6, P7, P10 |
| 13 | Model supply chain + media inputs: runtime downloads bypass manifest, pickles allowed, open egress, trainer invites docker.sock; FFmpeg whitelist inconsistent/allows `file`. Fix: full transitive manifest; `HF_HUB_OFFLINE=1`; safetensors/GGUF only; internal-only network for GPU containers (egress only for `models.pull`); no docker.sock anywhere; ComfyUI reachable only from worker; single FFmpeg whitelist constant, forced `-f`, `-safe 1`, Go-side host validation. (SEC3, SEC8) | High | Accept | P4, P7, P8, P9 |
| 14 | Data exposure & authz: capability URLs leak via logs; step logs viewer-readable; audit log not append-only (no role split, goose down drops it); tenant resolution unspecified, `pipeline_step_deps` lacks tenant_id; presigned POST key not pinned → reviewed segment swappable; CSP `style-src 'self'` breaks Radix/sonner/TipTap; ZAP unauthenticated. Fix: value-level URL scrubber + FFmpeg `-loglevel error`; log access editor+; `loomtale_owner`/`loomtale_app` roles, trigger blocking UPDATE/DELETE/TRUNCATE, refusing down; tenant only from session (`switch-tenant`), `tenant.FromCtx` analyzer, RLS planned; POST `eq $key`, workers-only `render/`, sha256 verify on cache hit + final-render hash at approval; CSP decision now (+ `object-src 'none'`, Trusted Types, CI check no Report-Only); authenticated ZAP API scan over TLS `*.localhost`. (SEC4–SEC10) | High | Accept | P2, P3, P5, P8, P10, P12 |
| 15 | Plan structure: 208h not credible (P9 alone 60–80h), no contingency; riskiest assumption (Blackwell ComfyUI/Qwen-Edit) tested at hour ~150; zoompan throughput on 4 vCPU unbenchmarked; `/gpu` schema co-edited by parallel phases; proto generated by two toolchains. Fix: 2h Blackwell smoke spike right after P1; split P9 into 9a image / 9b TTS+align / 9c LoRA+scoring; bottom-up re-estimate + 25–30% contingency; zoompan micro-benchmark gate in P8 step 2; full `GpuStatus` schema defined in P3; buf generates Go + Python stubs + cross-language round-trip test; fix compose file names in tech-stack.md. (AD7, AD5, SC7, SC10) | High | Accept | plan.md, P1, P3, P8, P9, P12 |

**Rejected (1):** SC9 "ZAP/k6/nightly/SSE benchmark exceed scope" — user explicitly set security and scale as top priority (contract §11); keep them (the SSE-cap inconsistency part is merged into #2).

## User decisions required
1. Raise `.wslconfig` limits (host change) — #4.
2. Edit during an active render: block vs supersede — #9.
3. CTR per video: add Reporting API (+6–8h) vs "CTR when available" — #3.
4. Backup target drive and Anthropic API-key budget fallback — #10, #11 (validation step).
