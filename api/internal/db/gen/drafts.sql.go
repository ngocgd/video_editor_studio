// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: drafts.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimDraftStepApplication = `-- name: ClaimDraftStepApplication :execrows
INSERT INTO draft_step_applications (step_id, tenant_id, draft_id, draft_version, applied_by)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (step_id) DO NOTHING
`

type ClaimDraftStepApplicationParams struct {
	StepID       pgtype.UUID `json:"step_id"`
	TenantID     pgtype.UUID `json:"tenant_id"`
	DraftID      pgtype.UUID `json:"draft_id"`
	DraftVersion int64       `json:"draft_version"`
	AppliedBy    pgtype.UUID `json:"applied_by"`
}

// Records that a step's result was applied; 0 rows means it already was.
func (q *Queries) ClaimDraftStepApplication(ctx context.Context, arg ClaimDraftStepApplicationParams) (int64, error) {
	result, err := q.db.Exec(ctx, claimDraftStepApplication,
		arg.StepID,
		arg.TenantID,
		arg.DraftID,
		arg.DraftVersion,
		arg.AppliedBy,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const createDraft = `-- name: CreateDraft :one
INSERT INTO episode_drafts (id, tenant_id, episode_id, lang, paragraphs, word_count)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at
`

type CreateDraftParams struct {
	ID         pgtype.UUID `json:"id"`
	TenantID   pgtype.UUID `json:"tenant_id"`
	EpisodeID  pgtype.UUID `json:"episode_id"`
	Lang       string      `json:"lang"`
	Paragraphs []byte      `json:"paragraphs"`
	WordCount  int32       `json:"word_count"`
}

func (q *Queries) CreateDraft(ctx context.Context, arg CreateDraftParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, createDraft,
		arg.ID,
		arg.TenantID,
		arg.EpisodeID,
		arg.Lang,
		arg.Paragraphs,
		arg.WordCount,
	)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createDraftIfAbsent = `-- name: CreateDraftIfAbsent :one
INSERT INTO episode_drafts (id, tenant_id, episode_id, lang, paragraphs, word_count)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (episode_id, lang) DO NOTHING
RETURNING id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at
`

type CreateDraftIfAbsentParams struct {
	ID         pgtype.UUID `json:"id"`
	TenantID   pgtype.UUID `json:"tenant_id"`
	EpisodeID  pgtype.UUID `json:"episode_id"`
	Lang       string      `json:"lang"`
	Paragraphs []byte      `json:"paragraphs"`
	WordCount  int32       `json:"word_count"`
}

// Used by the create-draft endpoint and by any AI action that needs a
// draft to write into (continue, expand_beat) but tolerates one already
// existing: zero rows back (no error) means a concurrent creator won and
// the caller should GetDraft instead of failing.
func (q *Queries) CreateDraftIfAbsent(ctx context.Context, arg CreateDraftIfAbsentParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, createDraftIfAbsent,
		arg.ID,
		arg.TenantID,
		arg.EpisodeID,
		arg.Lang,
		arg.Paragraphs,
		arg.WordCount,
	)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDraft = `-- name: GetDraft :one
SELECT id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at FROM episode_drafts WHERE tenant_id = $1 AND episode_id = $2 AND lang = $3
`

type GetDraftParams struct {
	TenantID  pgtype.UUID `json:"tenant_id"`
	EpisodeID pgtype.UUID `json:"episode_id"`
	Lang      string      `json:"lang"`
}

func (q *Queries) GetDraft(ctx context.Context, arg GetDraftParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, getDraft, arg.TenantID, arg.EpisodeID, arg.Lang)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getDraftByID = `-- name: GetDraftByID :one
SELECT id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at FROM episode_drafts WHERE tenant_id = $1 AND id = $2
`

type GetDraftByIDParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	ID       pgtype.UUID `json:"id"`
}

func (q *Queries) GetDraftByID(ctx context.Context, arg GetDraftByIDParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, getDraftByID, arg.TenantID, arg.ID)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertDraftRevision = `-- name: InsertDraftRevision :exec
INSERT INTO episode_draft_revisions (id, tenant_id, draft_id, version, paragraphs, word_count, created_by)
VALUES ($1, $2, $3, $4, $5, $6, $7)
`

type InsertDraftRevisionParams struct {
	ID         pgtype.UUID `json:"id"`
	TenantID   pgtype.UUID `json:"tenant_id"`
	DraftID    pgtype.UUID `json:"draft_id"`
	Version    int64       `json:"version"`
	Paragraphs []byte      `json:"paragraphs"`
	WordCount  int32       `json:"word_count"`
	CreatedBy  pgtype.UUID `json:"created_by"`
}

func (q *Queries) InsertDraftRevision(ctx context.Context, arg InsertDraftRevisionParams) error {
	_, err := q.db.Exec(ctx, insertDraftRevision,
		arg.ID,
		arg.TenantID,
		arg.DraftID,
		arg.Version,
		arg.Paragraphs,
		arg.WordCount,
		arg.CreatedBy,
	)
	return err
}

const listDraftRevisions = `-- name: ListDraftRevisions :many
SELECT id, tenant_id, draft_id, version, paragraphs, word_count, created_by, created_at FROM episode_draft_revisions
WHERE tenant_id = $1 AND draft_id = $2
ORDER BY version DESC
LIMIT $3
`

type ListDraftRevisionsParams struct {
	TenantID  pgtype.UUID `json:"tenant_id"`
	DraftID   pgtype.UUID `json:"draft_id"`
	PageLimit int32       `json:"page_limit"`
}

func (q *Queries) ListDraftRevisions(ctx context.Context, arg ListDraftRevisionsParams) ([]EpisodeDraftRevision, error) {
	rows, err := q.db.Query(ctx, listDraftRevisions, arg.TenantID, arg.DraftID, arg.PageLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []EpisodeDraftRevision
	for rows.Next() {
		var i EpisodeDraftRevision
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.DraftID,
			&i.Version,
			&i.Paragraphs,
			&i.WordCount,
			&i.CreatedBy,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const trimDraftRevisions = `-- name: TrimDraftRevisions :exec
DELETE FROM episode_draft_revisions AS outer_rev
WHERE outer_rev.tenant_id = $1
  AND outer_rev.draft_id = $2
  AND outer_rev.id NOT IN (
    SELECT inner_rev.id FROM episode_draft_revisions AS inner_rev
    WHERE inner_rev.draft_id = $2
    ORDER BY inner_rev.version DESC
    LIMIT 50
  )
`

type TrimDraftRevisionsParams struct {
	TenantID pgtype.UUID `json:"tenant_id"`
	DraftID  pgtype.UUID `json:"draft_id"`
}

// Keeps only the newest 50 revisions per draft; called after each insert.
func (q *Queries) TrimDraftRevisions(ctx context.Context, arg TrimDraftRevisionsParams) error {
	_, err := q.db.Exec(ctx, trimDraftRevisions, arg.TenantID, arg.DraftID)
	return err
}

const updateDraftParagraphs = `-- name: UpdateDraftParagraphs :one
UPDATE episode_drafts
SET paragraphs = $1,
    word_count = $2,
    version = $3,
    updated_at = now()
WHERE tenant_id = $4 AND id = $5 AND version = $6
RETURNING id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at
`

type UpdateDraftParagraphsParams struct {
	Paragraphs      []byte      `json:"paragraphs"`
	WordCount       int32       `json:"word_count"`
	NextVersion     int64       `json:"next_version"`
	TenantID        pgtype.UUID `json:"tenant_id"`
	ID              pgtype.UUID `json:"id"`
	ExpectedVersion int64       `json:"expected_version"`
}

// version = current_version + 1 is computed by the caller (story.Store)
// after loading and CAS-checking the row inside the same transaction, so
// the WHERE clause below is the actual optimistic-concurrency fence: a
// concurrent writer's UPDATE affects zero rows and the caller reports 409.
func (q *Queries) UpdateDraftParagraphs(ctx context.Context, arg UpdateDraftParagraphsParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, updateDraftParagraphs,
		arg.Paragraphs,
		arg.WordCount,
		arg.NextVersion,
		arg.TenantID,
		arg.ID,
		arg.ExpectedVersion,
	)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateDraftSummary = `-- name: UpdateDraftSummary :one
UPDATE episode_drafts
SET summary = $1,
    summary_tainted = $2,
    updated_at = now()
WHERE tenant_id = $3 AND id = $4
RETURNING id, tenant_id, episode_id, lang, paragraphs, version, word_count, summary, summary_tainted, created_at, updated_at
`

type UpdateDraftSummaryParams struct {
	Summary        string      `json:"summary"`
	SummaryTainted bool        `json:"summary_tainted"`
	TenantID       pgtype.UUID `json:"tenant_id"`
	ID             pgtype.UUID `json:"id"`
}

func (q *Queries) UpdateDraftSummary(ctx context.Context, arg UpdateDraftSummaryParams) (EpisodeDraft, error) {
	row := q.db.QueryRow(ctx, updateDraftSummary,
		arg.Summary,
		arg.SummaryTainted,
		arg.TenantID,
		arg.ID,
	)
	var i EpisodeDraft
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.EpisodeID,
		&i.Lang,
		&i.Paragraphs,
		&i.Version,
		&i.WordCount,
		&i.Summary,
		&i.SummaryTainted,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
